#pragma once
#include <string>
#include <filesystem>

inline std::filesystem::path current_path = "/home/step/LightningTree";
inline std::filesystem::path path_data = current_path / "LightningTree_data";